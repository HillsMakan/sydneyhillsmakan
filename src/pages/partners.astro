---
import { getCollection } from 'astro:content'
import { Image } from 'astro:assets'

import Layout from '../layouts/base.astro'
import Breadcrumb from '../components/breadcrumb.astro'
import Cards from '../components/cards.astro'

import PageImage from '../assets/images/meetourpartners.jpg'

const frontmatter = {
  title: 'Partners',
  description: 'All Our Partners.',
  image: PageImage
}

const partners = await getCollection('partner', (partner) => !partner.data.draft)
---

<Layout frontmatter={frontmatter}>
  <main data-pagefind-body class="h-auto p-4 pt-20 md:ml-64">
    <Breadcrumb links={[{ title: 'Partners', link: '/partners' }]} />
    <h1 class="mb-4 px-2 text-4xl font-extrabold tracking-tight text-gray-900 dark:text-white">
      Partners
    </h1>
    <Image class="px-2" src={PageImage} height={400} alt={frontmatter.title} />
    <Cards partners={partners} />
  </main>
</Layout>
